name = "bolt-chef-unified"
compatibility_date = "2024-01-01"

pages_build_output_dir = "dist"

[env.production]
vars = { ENVIRONMENT = "production" }
# Leave VITE_CONVEX_URL empty for now to use localhost during build
# VITE_CONVEX_URL will be set after Convex deployment

[env.preview]
vars = { ENVIRONMENT = "preview" }
# Leave VITE_CONVEX_URL empty for now to use localhost during build
